# URL of the word data JSON (relative to the app root or absolute).
# Default: /data.json
VITE_DATA_URL=/data.json

# Base URL of the pronunciation mp3s (no trailing slash).
# Default: /audio
VITE_AUDIO_BASE_URL=/audio
