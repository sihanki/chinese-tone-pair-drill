# URL of the word data JSON (relative or absolute). Required — nothing in the
# repo serves the files, so point this at where you host data.json.
VITE_DATA_URL=

# Base URL of the pronunciation mp3s (no trailing slash). Required.
VITE_AUDIO_BASE_URL=
